{
  // TypeScript configuration for the server-side code
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "types": ["@cloudflare/workers-types", "./worker-configuration.d.ts"] // Include Cloudflare Workers types and custom worker configuration types
  },
  "include": ["./index.ts", "./worker-configuration.d.ts"] // Include specific files for compilation
}
